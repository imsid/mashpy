[project]
name = "mashpy"
version = "0.1.2"
description = "Platform for building CLI-native agent applications."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Proprietary"}
authors = [{ name = "imsid" }]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "License :: Other/Proprietary License",
  "Operating System :: OS Independent",
]
keywords = ["agents", "cli", "mcp", "llm"]

dependencies = [
  "anthropic>=0.39.0",
  "mcp>=0.1.0",
  "openai>=1.57.4",
  "PyYAML>=6.0.2",
  "prompt_toolkit>=3.0.36",
  "rich>=13.7.1",
  "requests>=2.31.0",
  "tomli>=2.0.1; python_version < '3.11'",
]

[project.urls]
Homepage = "https://github.com/imsid/mashpy"
Documentation = "https://github.com/imsid/mashpy#readme"
Repository = "https://github.com/imsid/mashpy"

[project.scripts]
mash = "mash.cli.main:main"

[project.optional-dependencies]
dev = [
  "build>=1.2.2",
  "pytest>=8.3.0",
  "twine>=6.1.0",
]
telemetry-web = [
  "mash-telemetry-web>=0.1.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["mash*"]
exclude = ["apps*"]

[tool.uv.sources]
mash-telemetry-web = { path = "packages/mash-telemetry-web" }
